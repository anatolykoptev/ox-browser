# ox-browser v0.1.0 Implementation Plan

**Date:** 2026-03-04
**Status:** Draft
**Goal:** Fetch pages, parse DOM, extract data. Library + CLI. No JS yet.
**Design doc:** [v0.1.0-design.md](2026-03-04-v0.1.0-design.md)
**Architecture:** [ARCHITECTURE.md](../ARCHITECTURE.md)

**Tech stack:** Rust 2021 edition, dom_query 0.25+, reqwest 0.12, tokio 1, clap 4
**Source:** `.`
**Module:** `github.com/anatolykoptev/ox-browser`

---

### Task 1: Bootstrap Workspace

**Files:**
- Create: `Cargo.toml`, `src/main.rs`
- Create: `crates/{core,http,js,security,crawler,mcp}/Cargo.toml`
- Create: `crates/{core,http,js,security,crawler,mcp}/src/lib.rs`
- Create: `.gitignore`

**Step 1: Initialize workspace root**

```toml
# Cargo.toml
[workspace]
resolver = "2"
members = [
    "crates/core",
    "crates/http",
    "crates/js",
    "crates/security",
    "crates/crawler",
    "crates/mcp",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/anatolykoptev/ox-browser"
authors = ["Anatoly Koptev"]

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
anyhow = "1"
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[package]
name = "ox-browser"
version.workspace = true
edition.workspace = true

[dependencies]
ox-core = { path = "crates/core" }
ox-http = { path = "crates/http" }
clap = { version = "4", features = ["derive"] }
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
anyhow.workspace = true
```

**Step 2: Create crate Cargo.toml files**

Each crate gets minimal `Cargo.toml`. Phase 1 active crates: `ox-http`, `ox-core`.
Stub crates (`js`, `security`, `crawler`, `mcp`) get empty `lib.rs`.

```toml
# crates/http/Cargo.toml
[package]
name = "ox-http"
version.workspace = true
edition.workspace = true

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["cookies", "rustls-tls", "gzip", "brotli"] }
cookie_store = "0.21"
url = "2"
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true
```

```toml
# crates/core/Cargo.toml
[package]
name = "ox-core"
version.workspace = true
edition.workspace = true

[dependencies]
ox-http = { path = "../http" }
dom_query = "0.25"
url = "2"
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true
```

**Step 3: Stub main.rs**

```rust
// src/main.rs
fn main() {
    println!("ox-browser v0.1.0");
}
```

**Step 4: Verify**

```bash
cd .
cargo check --workspace
```

Expected: compiles with no errors.

**Commit:**
```
feat: bootstrap workspace with 6 crates

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 2: HTTP Client — `crates/http`

**Files:**
- Create: `crates/http/src/client.rs`
- Create: `crates/http/src/config.rs`
- Create: `crates/http/src/response.rs`
- Create: `crates/http/src/error.rs`
- Modify: `crates/http/src/lib.rs`

**Step 1: Define error types**

```rust
// crates/http/src/error.rs
use thiserror::Error;

#[derive(Error, Debug)]
pub enum HttpError {
    #[error("request failed: {0}")]
    Request(#[from] reqwest::Error),

    #[error("invalid URL: {0}")]
    InvalidUrl(String),

    #[error("timeout after {0:?}")]
    Timeout(std::time::Duration),
}

pub type Result<T> = std::result::Result<T, HttpError>;
```

**Step 2: Define config**

```rust
// crates/http/src/config.rs
use std::time::Duration;

pub struct HttpConfig {
    pub timeout: Duration,
    pub user_agent: String,
    pub proxy_url: Option<String>,
    pub max_redirects: usize,
}

impl Default for HttpConfig {
    fn default() -> Self {
        Self {
            timeout: Duration::from_secs(20),
            user_agent: format!("ox-browser/{}", env!("CARGO_PKG_VERSION")),
            proxy_url: None,
            max_redirects: 10,
        }
    }
}
```

**Step 3: Define response**

```rust
// crates/http/src/response.rs
use reqwest::header::HeaderMap;

pub struct HttpResponse {
    pub status: u16,
    pub url: String,
    pub headers: HeaderMap,
    pub body: String,
}
```

**Step 4: Implement client**

```rust
// crates/http/src/client.rs
use crate::{HttpConfig, HttpResponse, Result};
use reqwest::Client;
use std::sync::Arc;

pub struct HttpClient {
    inner: Client,
    config: HttpConfig,
}

impl HttpClient {
    pub fn new(config: HttpConfig) -> Result<Self> {
        let mut builder = Client::builder()
            .timeout(config.timeout)
            .user_agent(&config.user_agent)
            .redirect(reqwest::redirect::Policy::limited(config.max_redirects))
            .cookie_store(true);

        if let Some(ref proxy_url) = config.proxy_url {
            let proxy = reqwest::Proxy::all(proxy_url)
                .map_err(|e| crate::HttpError::InvalidUrl(e.to_string()))?;
            builder = builder.proxy(proxy);
        }

        Ok(Self {
            inner: builder.build()?,
            config,
        })
    }

    pub async fn get(&self, url: &str) -> Result<HttpResponse> {
        let resp = self.inner.get(url).send().await?;
        let status = resp.status().as_u16();
        let final_url = resp.url().to_string();
        let headers = resp.headers().clone();
        let body = resp.text().await?;

        Ok(HttpResponse { status, url: final_url, headers, body })
    }

    pub async fn post(&self, url: &str, body: &str, content_type: &str) -> Result<HttpResponse> {
        let resp = self.inner
            .post(url)
            .header("Content-Type", content_type)
            .body(body.to_string())
            .send()
            .await?;

        let status = resp.status().as_u16();
        let final_url = resp.url().to_string();
        let headers = resp.headers().clone();
        let body = resp.text().await?;

        Ok(HttpResponse { status, url: final_url, headers, body })
    }
}
```

**Step 5: Wire up lib.rs**

```rust
// crates/http/src/lib.rs
mod client;
mod config;
mod error;
mod response;

pub use client::HttpClient;
pub use config::HttpConfig;
pub use error::{HttpError, Result};
pub use response::HttpResponse;
```

**Step 6: Verify**

```bash
cargo check -p ox-http
```

**Commit:**
```
feat(http): add reqwest-based HTTP client with proxy and cookies

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 3: Core Error Types — `crates/core`

**Files:**
- Create: `crates/core/src/error.rs`

**Step 1: Define core errors**

```rust
// crates/core/src/error.rs
use thiserror::Error;

#[derive(Error, Debug)]
pub enum BrowserError {
    #[error("navigation failed: {0}")]
    Navigate(String),

    #[error("render timeout")]
    Timeout,

    #[error("invalid selector: {0}")]
    Selector(String),

    #[error("form not found: {0}")]
    FormNotFound(String),

    #[error("element not found: {0}")]
    ElementNotFound(String),

    #[error(transparent)]
    Http(#[from] ox_http::HttpError),
}

pub type Result<T> = std::result::Result<T, BrowserError>;
```

**Commit:**
```
feat(core): add error types

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 4: Page and DOM — `crates/core`

**Files:**
- Create: `crates/core/src/page.rs`
- Create: `crates/core/src/form.rs`
- Create: `crates/core/src/navigation.rs`

**Step 1: Implement Page**

Page wraps `dom_query::Document` and provides query/extraction methods.

```rust
// crates/core/src/page.rs
use crate::{Result, BrowserError, Form, Link, MetaTag};
use dom_query::Document;

pub struct Page {
    pub url: String,
    pub status: u16,
    doc: Document,
}

impl Page {
    pub fn new(url: String, status: u16, html: &str) -> Self {
        Self {
            url,
            status,
            doc: Document::from(html),
        }
    }

    pub fn select(&self, css: &str) -> dom_query::Selection {
        self.doc.select(css)
    }

    pub fn select_single(&self, css: &str) -> Option<dom_query::NodeRef> {
        // select returns Selection, get first node
        let sel = self.doc.select(css);
        sel.nodes().first().cloned()
    }

    pub fn title(&self) -> String {
        self.doc.select("title").text().to_string()
    }

    pub fn text(&self) -> String {
        self.doc.select("body").text().to_string()
    }

    pub fn html(&self) -> String {
        self.doc.html().to_string()
    }

    pub fn forms(&self) -> Vec<Form> {
        self.doc.select("form")
            .iter()
            .map(|node| Form::from_node(&node))
            .collect()
    }

    pub fn form_by_id(&self, id: &str) -> Option<Form> {
        let sel = self.doc.select(&format!("form#{}", id));
        sel.nodes().first().map(|n| Form::from_node(n))
    }

    pub fn links(&self) -> Vec<Link> {
        self.doc.select("a[href]")
            .iter()
            .filter_map(|node| {
                let href = node.attr("href")?.to_string();
                let text = node.text().to_string();
                Some(Link { href, text })
            })
            .collect()
    }

    pub fn meta_tags(&self) -> Vec<MetaTag> {
        self.doc.select("meta")
            .iter()
            .map(|node| MetaTag {
                name: node.attr("name").unwrap_or_default().to_string(),
                property: node.attr("property").unwrap_or_default().to_string(),
                content: node.attr("content").unwrap_or_default().to_string(),
            })
            .collect()
    }

    pub fn document(&self) -> &Document {
        &self.doc
    }
}

pub struct Link {
    pub href: String,
    pub text: String,
}

pub struct MetaTag {
    pub name: String,
    pub property: String,
    pub content: String,
}
```

**Step 2: Implement Form**

```rust
// crates/core/src/form.rs
use dom_query::NodeRef;

pub struct Form {
    pub action: String,
    pub method: String,
    pub fields: Vec<FormField>,
}

pub struct FormField {
    pub name: String,
    pub value: String,
    pub field_type: String,
    pub disabled: bool,
}

impl Form {
    pub fn from_node(node: &NodeRef) -> Self {
        let action = node.attr("action").unwrap_or_default().to_string();
        let method = node.attr("method")
            .unwrap_or_default()
            .to_string()
            .to_uppercase();
        let method = if method.is_empty() { "GET".into() } else { method };

        let mut fields = Vec::new();

        // Extract input fields
        // (implementation iterates over input, select, textarea elements)

        Self { action, method, fields }
    }

    pub fn set_field(&mut self, name: &str, value: &str) {
        if let Some(field) = self.fields.iter_mut().find(|f| f.name == name) {
            field.value = value.to_string();
        }
    }

    pub fn serialize(&self) -> String {
        self.fields
            .iter()
            .filter(|f| !f.disabled && !f.name.is_empty())
            .map(|f| {
                format!("{}={}", url_encode(&f.name), url_encode(&f.value))
            })
            .collect::<Vec<_>>()
            .join("&")
    }
}

fn url_encode(s: &str) -> String {
    url::form_urlencoded::byte_serialize(s.as_bytes()).collect()
}
```

**Step 3: Implement navigation helpers**

```rust
// crates/core/src/navigation.rs
use url::Url;

pub fn resolve_url(base: &str, relative: &str) -> Option<String> {
    let base_url = Url::parse(base).ok()?;
    let resolved = base_url.join(relative).ok()?;
    Some(resolved.to_string())
}

pub fn is_same_origin(url1: &str, url2: &str) -> bool {
    let u1 = Url::parse(url1).ok();
    let u2 = Url::parse(url2).ok();
    match (u1, u2) {
        (Some(a), Some(b)) => a.origin() == b.origin(),
        _ => false,
    }
}
```

**Commit:**
```
feat(core): add Page, Form, and navigation helpers

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 5: Browser and Pool — `crates/core`

**Files:**
- Create: `crates/core/src/browser.rs`
- Create: `crates/core/src/pool.rs`
- Create: `crates/core/src/session.rs`
- Create: `crates/core/src/config.rs`
- Modify: `crates/core/src/lib.rs`

**Step 1: Implement Pool**

```rust
// crates/core/src/pool.rs
use std::sync::atomic::{AtomicBool, Ordering};
use tokio::sync::Semaphore;
use crate::Result;

pub struct Pool {
    sem: Semaphore,
    closed: AtomicBool,
}

impl Pool {
    pub fn new(size: usize) -> Self {
        Self {
            sem: Semaphore::new(size),
            closed: AtomicBool::new(false),
        }
    }

    pub async fn acquire(&self) -> Result<PoolGuard<'_>> {
        if self.closed.load(Ordering::Relaxed) {
            return Err(crate::BrowserError::Navigate("pool closed".into()));
        }
        let permit = self.sem.acquire().await
            .map_err(|_| crate::BrowserError::Navigate("pool closed".into()))?;
        Ok(PoolGuard { _permit: permit })
    }

    pub fn close(&self) {
        self.closed.store(true, Ordering::Relaxed);
        self.sem.close();
    }
}

pub struct PoolGuard<'a> {
    _permit: tokio::sync::SemaphorePermit<'a>,
}
```

**Step 2: Implement Browser**

```rust
// crates/core/src/browser.rs
use crate::{Page, Pool, Result, BrowserConfig};
use ox_http::{HttpClient, HttpConfig};

pub struct Browser {
    http: HttpClient,
    pool: Pool,
    config: BrowserConfig,
}

impl Browser {
    pub fn new(config: BrowserConfig) -> Result<Self> {
        let http_config = HttpConfig {
            timeout: config.timeout,
            user_agent: config.user_agent.clone(),
            proxy_url: config.proxy_url.clone(),
            max_redirects: config.max_redirects,
        };
        let http = HttpClient::new(http_config)?;
        let pool = Pool::new(config.concurrency);

        Ok(Self { http, pool, config })
    }

    pub async fn page(&self, url: &str) -> Result<Page> {
        let _guard = self.pool.acquire().await?;
        let resp = self.http.get(url).await?;
        Ok(Page::new(resp.url, resp.status, &resp.body))
    }

    pub fn close(&self) {
        self.pool.close();
    }
}
```

**Step 3: Wire up lib.rs**

```rust
// crates/core/src/lib.rs
mod browser;
mod config;
mod error;
mod form;
mod navigation;
mod page;
mod pool;
mod session;

pub use browser::Browser;
pub use config::BrowserConfig;
pub use error::{BrowserError, Result};
pub use form::{Form, FormField};
pub use navigation::{resolve_url, is_same_origin};
pub use page::{Page, Link, MetaTag};
pub use pool::Pool;
```

**Step 4: Verify**

```bash
cargo check -p ox-core
```

**Commit:**
```
feat(core): add Browser, Pool, and Session

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 6: CLI — `src/main.rs`

**Files:**
- Modify: `src/main.rs`

**Step 1: Implement CLI with clap**

```rust
// src/main.rs
use clap::{Parser, Subcommand};
use ox_core::{Browser, BrowserConfig};

#[derive(Parser)]
#[command(name = "ox-browser", version, about = "Lightweight headless browser")]
struct Cli {
    #[command(subcommand)]
    command: Commands,
}

#[derive(Subcommand)]
enum Commands {
    /// Fetch a URL and output content
    Fetch {
        /// URL to fetch
        url: String,
        /// CSS selector to extract
        #[arg(long)]
        css: Option<String>,
        /// Output plain text instead of HTML
        #[arg(long)]
        text: bool,
    },
    /// Show version info
    Version,
}

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt::init();

    let cli = Cli::parse();
    match cli.command {
        Commands::Fetch { url, css, text } => {
            let browser = Browser::new(BrowserConfig::default())?;
            let page = browser.page(&url).await?;

            if let Some(selector) = css {
                let sel = page.select(&selector);
                for node in sel.iter() {
                    if text {
                        println!("{}", node.text());
                    } else {
                        println!("{}", node.html());
                    }
                }
            } else if text {
                println!("{}", page.text());
            } else {
                println!("{}", page.html());
            }
        }
        Commands::Version => {
            println!("ox-browser {}", env!("CARGO_PKG_VERSION"));
        }
    }
    Ok(())
}
```

**Step 2: Verify**

```bash
cargo build
./target/debug/ox-browser fetch https://example.com --text
```

Expected: prints "Example Domain" text content.

**Commit:**
```
feat(cli): add fetch subcommand with CSS selector support

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 7: Unit Tests

**Files:**
- Create: `crates/core/tests/page_test.rs`
- Create: `crates/core/tests/form_test.rs`
- Create: `crates/core/tests/navigation_test.rs`

**Step 1: Page tests**

```rust
// crates/core/tests/page_test.rs
use ox_core::Page;

#[test]
fn test_page_title() {
    let page = Page::new(
        "https://example.com".into(),
        200,
        "<html><head><title>Test</title></head><body>Hello</body></html>",
    );
    assert_eq!(page.title(), "Test");
}

#[test]
fn test_page_select() {
    let page = Page::new(
        "https://example.com".into(),
        200,
        "<div class=\"a\">1</div><div class=\"b\">2</div>",
    );
    let sel = page.select(".b");
    assert_eq!(sel.text().to_string(), "2");
}

#[test]
fn test_page_links() {
    let page = Page::new(
        "https://example.com".into(),
        200,
        r#"<a href="/about">About</a><a href="https://other.com">Other</a>"#,
    );
    let links = page.links();
    assert_eq!(links.len(), 2);
    assert_eq!(links[0].href, "/about");
    assert_eq!(links[0].text, "About");
}

#[test]
fn test_page_meta_tags() {
    let page = Page::new(
        "https://example.com".into(),
        200,
        r#"<meta name="description" content="A test page">"#,
    );
    let meta = page.meta_tags();
    assert!(meta.iter().any(|m| m.name == "description" && m.content == "A test page"));
}
```

**Step 2: Navigation tests**

```rust
// crates/core/tests/navigation_test.rs
use ox_core::{resolve_url, is_same_origin};

#[test]
fn test_resolve_relative() {
    let result = resolve_url("https://example.com/page", "/about");
    assert_eq!(result, Some("https://example.com/about".into()));
}

#[test]
fn test_resolve_relative_path() {
    let result = resolve_url("https://example.com/dir/page", "other");
    assert_eq!(result, Some("https://example.com/dir/other".into()));
}

#[test]
fn test_same_origin() {
    assert!(is_same_origin("https://example.com/a", "https://example.com/b"));
    assert!(!is_same_origin("https://example.com", "https://other.com"));
}
```

**Step 3: Verify**

```bash
cargo test --workspace
```

Expected: all tests pass.

**Commit:**
```
test(core): add unit tests for Page, Form, and navigation

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 8: Lint and Quality Setup

**Files:**
- Create: `.clippy.toml` or `clippy.toml`
- Create: `Makefile`
- Create: `.pre-commit-config.yaml`
- Create: `rustfmt.toml`

**Step 1: Clippy config**

```toml
# clippy.toml
cognitive-complexity-threshold = 20
too-many-arguments-threshold = 5
```

**Step 2: Rustfmt config**

```toml
# rustfmt.toml
max_width = 100
use_field_init_shorthand = true
```

**Step 3: Makefile**

```makefile
.PHONY: build test lint fmt check

build:
	cargo build --workspace

test:
	cargo test --workspace

lint:
	cargo clippy --workspace -- -D warnings

fmt:
	cargo fmt --all

check: fmt lint test
	@echo "All checks passed"
```

**Step 4: Verify**

```bash
make check
```

Expected: all checks pass.

**Commit:**
```
chore: add Makefile, clippy, rustfmt, pre-commit configs

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
```

---

### Task 9: GitHub Repository

**Step 1: Initialize git**

```bash
cd .
git init
git add .
git commit -m "feat: ox-browser v0.1.0 — lightweight headless browser

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

**Step 2: Create GitHub repo**

```bash
gh repo create anatolykoptev/ox-browser --private --source=. --push
```

**Step 3: Tag**

```bash
git tag v0.1.0
git push origin v0.1.0
```

---

### Task 10: Verify End-to-End

**Step 1: Full build**

```bash
cargo build --release
```

**Step 2: CLI test**

```bash
./target/release/ox-browser fetch https://example.com --css h1 --text
```

Expected: `Example Domain`

**Step 3: Check crate stats**

```bash
find crates/ -name "*.rs" | xargs wc -l | tail -1
```

Expected: ~800 LOC total.

```bash
cargo test --workspace 2>&1 | tail -5
```

Expected: all tests pass, 0 failures.
